import React from 'react';
import { Icon } from 'commons/fragments';

function App() {
  return (
    <div className="App">
      <Icon name={'add'} />
      <Icon name={'check'} />
      <Icon name={'close'} />
      <Icon name={'down-arrow'} />
      <Icon name={'pen'} />
      <Icon name={'play'} />
      <Icon name={'rewind'} />
      <Icon name={'step'} />
    </div>
  );
}

export default App;
